# Shiro Memory

Local, durable second-brain tools for Shiro. Data is stored only under the
explicit `SHIRO_MEMORY_ROOT`; the plugin never accepts filesystem paths from
the model.

Tools: `memory_store`, `memory_search`, `memory_get`, `memory_list`,
`memory_forget`, and `memory_restore`.
